---
feature: F-026
actualizado: 2026-08-31T05:40:47Z
branch: adrianfdez469/menhaden
ciclo: 2
---

## Estado actual

Construido y verificado. La tienda pública tiene el selector de categorías sobre
la rejilla y la vista `/[slug]/c/[categorySlug]`, pre-renderizada, sin una sola
consulta nueva y con cero JavaScript de cliente. `LocalCategory` tiene el slug
único por negocio y su guarda anti-rancia, y un evento `CATEGORY` del sync por
fin invalida las sucursales que toca — antes no invalidaba nada.

`tests.md` está en `veredicto: listo` con los 15 criterios ejecutados, y
`bash .agent/verify.sh F-026 --full` y `--smoke` salen en 0. Lo único que falta
para cerrar es la firma del humano en `.agent/features.json`.

## Criterios cubiertos

Los quince, verificados por `sdd-tester` ejecutando algo real —peticiones HTTP
con su código de estado, eventos de sync de verdad contra
`/api/internal/sync/catalog`, el log crudo del build y un clic en el navegador—,
ninguno leyendo código. El detalle, con la salida de cada comando, en
`.agent/specs/F-026/tests.md` § Comandos exactos por criterio.

- [x] 1 — enlace por categoría con stock → `curl /tienda-demo` 200; los cuatro `href` y ninguno de una categoría sin producto ahí, reforzado con `bodega-dos`
- [x] 2 — la vista lista solo lo suyo → `curl /tienda-demo/c/bebidas` 200; las 4 bebidas presentes, los 3 de «Aseo» con `grep -c` = 0
- [x] 3 — visibilidad → `PRODUCT`/`UPDATE` con `publishToStore:false` la quita; `POST /api/internal/sync/availability` con `OUT_OF_STOCK` la deja con `>Agotado<`
- [x] 4 — `priceOverride` → los dos `<p>` de precio extraídos por script y comparados como cadenas: `'$1,150.00' == '$1,150.00'`
- [x] 5 — categoría de otra sucursal → `curl -w '%{http_code}' /bodega-uno/c/aseo` → **404**, igual que `/tienda-demo/c/no-existe-123`
- [x] 6 — selector y tienda cerrada → `/el-trebol/c/lo-que-sea` → 404; `/tienda-cerrada/c/<slug inventado>` → 200 con el aviso, que es la prueba de que no consultó catálogo
- [x] 7 — precio tras sync → `send-catalog-batch.mjs` real (450→499): `$499.00` en `/tienda-demo` **y** en `/tienda-demo/c/bebidas`
- [x] 8 — cambio de categoría → sync real: sale de `/c/bebidas` y entra en `/c/alimentos` en la misma comprobación
- [x] 9 — `CATEGORY`/`DELETE` → «Panadería» fuera del selector, `/c/panaderia` → 404, y sus 3 productos siguen en `/tienda-demo`
- [x] 10 — `CATEGORY`/`UPDATE` → «Aseo» → «Higiene y limpieza»: nombre nuevo en el chip y el `<h1>`, `/c/aseo` sigue en 200
- [x] 11 — slugs homónimos → `CATEGORY`/`CREATE` de «Café» y «CAFÉ» → `cafe` y `cafe-2`, cada URL con solo su producto
- [x] 12 — SSG → log crudo del build: las rutas concretas de `/[slug]` llevan **●**, y `/[slug]/c/[categorySlug]` pre-renderiza 14
- [x] 13 — coste de cliente → `check-bundle-budget.mjs` en 0 (177.6 / 193 KB) y `grep -rn "use client"` sobre los 11 archivos del feature, vacío
- [x] 14 — sin JavaScript → `curl`, que nunca ejecuta JS, sigue el `<a href>` real hasta la vista y encuentra nombre y precio en el HTML crudo
- [x] 15 — el sensor → `bash .agent/verify.sh F-026 --full` en **0**, y `--smoke` en 0 con 44 aserciones

## Decisiones tomadas

**Del humano, el 2026-08-30/31** (las de 2026-08-29 —SP1 a SP4— están en
`.agent/specs/F-026/spec.md` § Huecos y preguntas al humano):

- **La migración es aditiva y sin reset**: `prisma migrate dev --create-only`,
  se borran a mano los cinco `DROP INDEX` de índices GIN, backfill en la misma
  migración. Nunca `prisma migrate reset` ni `prisma db push`.
- **PP1 = (a)**: se acepta proyectar `categorySlug` en la consulta de búsqueda de
  F-021, que la spec había dejado fuera de alcance. Solo la columna; ni el
  ranking, ni las tres capas, ni la paginación.
- **PP2 = (a)**: el contraste 3.33 : 1 del chip activo se acepta como deuda
  heredada —ya afecta a `Buscar` y a `Agregar al carrito`— y el chip **no**
  forkea el lenguaje visual. Queda pendiente que el humano abra el feature que
  valide el contraste al guardar la marca; es suyo por la regla 4.
- **PP3 = solo 5b**: entra el enlace a la categoría desde la ficha del producto;
  **no** entran las vistas de categoría en el sitemap.

**Del arquitecto** (`architecture.md`), las que no se deducen del código:

- La vista por categoría **no abre ninguna consulta**: es una proyección en
  memoria de `getStoreCatalog()`. Promovido a
  `docs/adr/0025-recortes-del-catalogo-como-proyeccion.md`, porque quien lo va a
  reabrir sin querer es F-027.
- **Se cierra I8** (guarda anti-rancia de `CATEGORY`) con una columna
  `sourceUpdatedAt` nullable, sin tocar `docs/sync-contract.md`.
- Se pre-renderiza la ruta: 13 pares (sucursal, categoría) hoy, contra las 30
  páginas de producto que el mismo build ya emite.

**Del diseñador** (`design.md`), las dos que la spec le delegaba:

- El selector es una **fila de chips desplazable a 360 px que envuelve desde
  `sm:`**, con `Todo el catálogo` de primer chip. Medido: 15 categorías
  envolviendo ocupan 372 px y echan la rejilla fuera de pantalla; deslizando,
  52 px.
- **`ProductCard` no muestra la categoría y no se toca**: envuelve todo su
  contenido en un único enlace, así que el nombre sería texto muerto con pinta
  de tocable.

## Problemas resueltos en este ciclo

Qué falló en la verificación y cómo se arregló. Uno por línea, con la firma que
imprimió `verify.sh` y —si dio lección— la ficha de `.agent/playbook/` donde
quedó escrita. `sdd.sh done` no cierra el feature mientras quede un fallo que no
esté ni fichado ni descartado.

- `format:Code style issues found in the above file(s)` sobre
  `.agent/progress/F-026.md` → prettier convirtió una cursiva escrita con
  asteriscos en una escrita con guiones bajos, dentro de una entrada de bitácora
  que **describía ese mismo cambio**, y dejó la frase diciendo «X → X». Es
  `.agent/playbook/prettier-write-reescribe-prosa-ajena.md`: ya estaba fichado,
  se leyó y se aplicó su arreglo (reescribir la línea a mano). Sin lección nueva.
- `smoke:QAB_BEARER_TOKEN no está configurado` → el guion de humo envía eventos
  de sync de verdad y necesita un bearer token que no está —ni puede estar— en
  el repo. `sdd.sh start` solo lo avisa con `!`, así que el entorno sale
  `ENTORNO LISTO` y el fallo aparece media hora después. **Ficha nueva:**
  `.agent/playbook/smoke-sin-token-de-sync.md`. Arreglado acuñándolo con
  `npm run mint:token -- seed-negocio-1` y pegándolo en `.env`.

- `format:Code style issues found in the above file(s)` sobre
  `.agent/progress/F-026.md` → prettier convirtió `*cursiva*` en `_cursiva_`
  dentro de una entrada de bitácora que **describía** ese mismo cambio, y dejó la
  frase diciendo «X → X». Es exactamente
  `.agent/playbook/prettier-write-reescribe-prosa-ajena.md`: ya estaba fichado,
  se leyó y se aplicó su arreglo (reescribir la línea a mano para que el signo no
  quede al principio). Sin lección nueva.

## Bloqueado por

Nada, o el qué y el quién.

## Próximo paso concreto

El humano pone `"passes": true` en la entrada de F-026 de `.agent/features.json`
—es suyo, y es el único punto donde alguien afirma que el feature existe— y
entonces `bash .agent/sdd.sh done F-026`.

<!-- Cerrado el ciclo. El paso que quedó atrás, por si hubiera que rehacerlo:
Ejecutar el **paso 1** de `.agent/specs/F-026/plan.md`: añadir a
`LocalCategory` en `prisma/schema.prisma` (líneas 303-318) el campo
`sourceUpdatedAt DateTime?` y el `@@unique([businessId, slug])`, generar la
migración con `npx prisma migrate dev --create-only`, **borrar a mano del
`migration.sql` las cinco líneas `DROP INDEX`** de los índices GIN que el schema
no declara, pegar el backfill que `.agent/specs/F-026/architecture.md` § El SQL
que hay que revisar a mano deja escrito entero, y solo entonces aplicar con
`npm run db:migrate`. — HECHO el 2026-08-31. -->

## Notas para quien retome

- **El entorno de este worktree se montó el 2026-08-31**: faltaban `npm ci`, el
  cliente de Prisma y `.env`. El `.env` se copió del checkout principal
  (`/Users/adrian/Documents/PROJECTS/cuadre-caja/queandabuscando/.env`), **no**
  de `.env.example`: así las claves locales de Storage cuadran con los
  contenedores que ya estaban levantados. Generarlas de nuevo aquí las
  desincroniza.
- **El puerto 3000 lo ocupaba un `next dev` de otro checkout**
  (`.orca-worktree-trash`), que es literalmente la trampa de `AGENTS.md` § Cosas
  que muerden. `sdd-designer` levantó el suyo en el 3100 y midió contra ese; al
  apagarlo, un `pkill` demasiado amplio se llevó también el del 3000. Comprueba
  el directorio del proceso antes de creerte lo que ves en el navegador.
- **El paso 3 del plan no sale de la spec.** Salió de que el arquitecto
  descubrió que `handleCategory` devuelve `PROCESSED` pelado
  (`src/features/sync/server/handlers/misc.ts`, sus cuatro salidas) y que
  `HandlerOutcome` solo tiene `touchedStoreSlug` en singular: hoy un evento
  `CATEGORY` **no invalida nada**, y sin cerrarlo los criterios 9 y 10 son
  inalcanzables. Verificado a mano por el orquestador, no solo reportado.
- **El paso 1 es el único irreversible.** Una migración aplicada no se deshace
  borrando el archivo; si hay que retroceder, se escribe otra que quite el
  unique.

## Bitácora

Append-only, en orden cronológico. Una entrada por agente que trabaja, escrita
por él mismo al terminar con `bash .agent/sdd.sh log <id> <agente>`.

El estado de los artefactos no se anota aquí: lo deriva `sdd.sh status` del
frontmatter de cada uno.

### 2026-08-29T03:42:43Z — sdd-spec

- Hizo: trasladó .agent/specs/propuestas/categorias-y-subcategorias.md a spec.md, resolviendo SP1/SP2/SP3/SP4 con la recomendación del agente (aceptada por el humano) y marcando los 15 criterios como [ya].
- Escribió: .agent/specs/F-026/spec.md (estado: listo)
- Deja pendiente: architecture.md y design.md en blanco. La migración de LocalCategory (@@unique[businessId, slug]) queda para sdd-architect.
- Siguiente agente sugerido: sdd-architect (necesita diseñar la migración aditiva y la ruta /[slug]/c/[categorySlug])

### 2026-08-31T03:17:17Z — sdd-architect

- Hizo: decidió los seis puntos que la spec delegó a arquitectura (ruta y estrategia de render, función de lectura, derivación del selector, invalidación, migración aditiva e I8), con las cifras consultadas contra el Postgres de desarrollo: 13 pares (sucursal publicada, categoría con producto visible), 14 params de generateStaticParams, 0 colisiones de (businessId, slug) en LocalCategory hoy. Hallazgo central: handleCategory devuelve PROCESSED pelado y hoy un evento CATEGORY no invalida NADA, así que los criterios 9 y 10 son inalcanzables sin ampliarlo.
- Escribió: .agent/specs/F-026/architecture.md (estado: listo) y el borrador docs/adr/0025-recortes-del-catalogo-como-proyeccion.md (estado: Propuesta).
- Deja pendiente: AP1 (añadir categorySlug a CatalogProduct obliga a proyectar una columna más en la consulta de búsqueda de F-021, que la spec dejó fuera de alcance; recomendación (a), aceptarlo). design.md lo escribe sdd-designer en paralelo. Nada de src/, prisma/ ni scripts/ tocado: la migración queda escrita, no aplicada.
- Verificación: bash .agent/verify.sh F-026 → 0. npm run check:harness → 0. npx prettier --check sobre los dos archivos escritos → limpio.
- Siguiente agente sugerido: el orquestador destila plan.md con AP1 resuelto; luego sdd-implementer (etapa 1 = migración + handleCategory; etapa 2 = lectura y ruta).

### 2026-08-31T03:19:06Z — sdd-designer

- Hizo: cerró la forma del selector y la vista por categoría con la pantalla real delante — `next dev` propio en el 3100 (el 3000 lo ocupaba OTRO checkout, `.orca-worktree-trash`), Playwright a 360/768/1280 en claro y oscuro sobre tienda-demo y tienda-dos, con el selector prototipado inyectado en la página real y medido: 15 categorías envolviendo = 372 px a 360 px (la rejilla cae fuera de pantalla) frente a 52 px desplazándose. Decidió: fila de chips desplazable a 360 y que envuelve desde sm:, siempre justo encima de la rejilla en las dos pantallas; primer chip «Todo el catálogo» (R6 por construcción, y marca de activo en /[slug]); la tarjeta del catálogo NO muestra la categoría (ProductCard no se toca: su Link envuelve todo y un `<a>` dentro de otro es HTML inválido); `<h1>` = nombre de la categoría + línea de conteo; sin estado vacío (cero productos es 404) y con `not-found.tsx` propio en el segmento para no perder el marco de la tienda tras un CATEGORY/DELETE. Midió contrastes: chip activo 4.84:1 en la marca por defecto y 3.33:1 en tienda-dos (heredado de `bg-brand`/`text-brand-contrast`, el mismo del botón Buscar) — sube como DP1 en vez de esconderlo.
- Escribió: .agent/specs/F-026/design.md (estado: listo). `npm run format` sobre ese archivo (solo realineó tablas y cambió una cursiva escrita con asteriscos por la misma cursiva escrita con guiones bajos), `npm run check:harness` en 0 y `bash .agent/verify.sh F-026` en 0.
- Deja pendiente: DP1 (contraste 3.33:1 del chip activo: aceptarlo y abrir la validación de contraste en storeTheme.ts, recomendado), DP2 (convertir el nombre de la categoría de la ficha de producto en enlace, recomendado sí), DP3 (meter las vistas de categoría en sitemap.ts, recomendado sí). Y ocho requisitos para el arquitecto, RD1–RD8, entre ellos: la lectura debe devolver `{ slug, name }` por categoría con el MISMO filtro de visibilidad y colación española, SIN conteo (los chips no llevan número); cero productos ⇒ notFound(); los chips con el prefetch de next/link apagado; `not-found.tsx` no recibe params, así que su enlace de salida es relativo («../..»).
- Siguiente agente sugerido: el orquestador, para destilar plan.md conciliando este documento con architecture.md (que sigue en borrador) y subir DP1–DP3 al humano.

### 2026-08-31T03:30:06Z — humano

- Hizo: aprobó el plan
- Escribió: .agent/specs/F-026/plan.md (aprobado: sí)
- Dijo: «Apruebo. PP1 = (a) sí, proyectar la columna en la consulta de búsqueda. PP2 = (a) aceptar el contraste heredado aquí y arreglar la raíz en un feature aparte. PP3 = entra solo el enlace en la ficha del producto (5b); las categorías NO entran en el sitemap. La migración, aditiva y sin reset.»
- Siguiente agente sugerido: sdd-implementer (el plan ya es ejecutable)

### 2026-08-31T03:43:54Z — sdd-implementer

- Hizo: implementó los pasos 1–3 de plan.md (migración de LocalCategory, slug sin colisión con guarda anti-rancia en handleCategory, y que un evento CATEGORY reporte qué revalidar). Los pasos 4–8 y 5b quedan para el segundo ciclo, sin tocar.
- Escribió: prisma/schema.prisma (LocalCategory: sourceUpdatedAt, @@unique([businessId, slug])), prisma/migrations/20260831033437_local_category_slug_unique/migration.sql (aditiva, con los DROP INDEX de los índices GIN borrados a mano y el backfill de architecture.md pegado), src/constants/catalog.ts (nuevo), src/lib/slug.ts (uniqueSlug gana honorReserved, default true), src/features/sync/server/handlers/misc.ts (handleCategory reescrito: guarda anti-rancia I8, slug sin colisión en el CREATE con reintento ante P2002, slug intocado en el UPDATE, affectedStoreSlugs() antes de escribir/borrar), src/features/sync/server/handlers/types.ts (HandlerOutcome.touchedStoreSlugs, plural), src/features/sync/server/processBatch.ts (lo funde en el mismo Set que revalidateStores), y los tests: src/features/sync/server/handlers/misc.test.ts (nuevo, 14 tests) y +4 tests en src/features/sync/server/processBatch.test.ts. Y .agent/specs/F-026/impl.md.
- Verificación: npx prisma validate → 0. Migración generada con --create-only (vía `expect`, porque el entorno no tiene TTY y prisma migrate dev exige uno para el prompt de confirmación) y aplicada con npm run db:migrate. Comprobado contra la base (docker exec queandabuscando-postgres psql): existe LocalCategory_businessId_slug_key y los cinco índices GIN (CanonicalProduct_searchVector_idx, CanonicalProduct_name_trgm_idx, StoreProduct_visible_catalog_idx, StoreProduct_searchVector_idx, StoreProduct_searchDocument_trgm_idx) siguen ahí, antes y después de aplicar. npx prisma migrate status → up to date. bash .agent/verify.sh F-026 → 0 (typecheck·lint·format·test). npm run check:harness → 0. bash .agent/verify.sh pending F-026 → sin salida (nada que fichar ni descartar).
- Deja pendiente: docs/despliegue.md sin la línea de esta migración (es del paso 7, del segundo ciclo, deliberadamente no tocado); StoreProduct_visible_catalog_idx no apareció esta vez en el DROP INDEX que Prisma propuso (a diferencia de los otros cuatro) — sigue existiendo, comprobado, pero si un futuro `migrate dev` lo propone hay que aplicar la misma ficha del playbook.
- Siguiente agente sugerido: sdd-implementer, segundo ciclo (pasos 4–7 más 5b: la lectura, el selector, la vista por categoría, el enlace en la ficha del producto, la documentación operativa y el cierre), con la migración ya aplicada en este entorno.

### 2026-08-31T03:58:58Z — sdd-implementer

- Hizo: implementó los pasos 4, 5, 6, 5b y 7 de plan.md (segundo ciclo, con la migración del primer ciclo ya aplicada): la lectura (categorySlug en CatalogProduct, deriveStoreCategories/productsOfCategory/storeCategoryPath puros, getStoreCategories/getStoreCategoryView como envoltorios sobre getStoreCatalog, categorySlug proyectado en search.ts por PP1), el selector StoreCategoryNav montado por /[slug] (solo con >=2 categorías) y por la vista de categoría, la ruta estática /[slug]/c/[categorySlug] con su generateStaticParams/generateMetadata y su not-found.tsx propio, el enlace de la categoría en la ficha del producto (5b), y la documentación operativa (docs/despliegue.md + ADR 0025 Propuesta→Aceptada). Cerró con el paso 8: bash .agent/verify.sh F-026 --full en 0, con /[slug] confirmado ● (SSG) en la salida cruda del build y /[slug]/c/[categorySlug] pre-renderizada con 14 rutas.
- Escribió: src/features/catalog/storeCategories.ts (nuevo) y storeCategories.test.ts (nuevo, 9 tests), src/features/catalog/server/queries.ts (categorySlug + getStoreCategories/getStoreCategoryView), src/features/catalog/server/search.ts (categorySlug proyectado, PP1), src/constants/catalog.ts (CATEGORY_ROUTE_SEGMENT), src/components/store/StoreCategoryNav.tsx (nuevo), src/app/[slug]/page.tsx (monta el selector), src/app/[slug]/c/[categorySlug]/page.tsx y not-found.tsx (nuevos), src/app/[slug]/p/[productSlug]/page.tsx (5b), docs/despliegue.md, docs/adr/0025-recortes-del-catalogo-como-proyeccion.md (Aceptada), y .agent/specs/F-026/impl.md (ampliado con § Segundo ciclo, sin reescribir el primero).
- Desvió: el not-found.tsx del segmento usa href=".." y no "../.." como decía literalmente design.md/RD4 — verificado con new URL() (WHATWG) que next/link en el app router pasa el href tal cual al <a> y el navegador lo resuelve contra la URL actual; "../.." habría mandado a la raíz del sitio en vez de a la tienda. Mismo criterio y misma profundidad que src/app/[slug]/pedido/[code]/not-found.tsx, que ya usa un solo "..". Queda documentado en impl.md § Desviaciones.
- Verificación: bash .agent/verify.sh F-026 → 0 (intento 15; el 14 falló en format sobre tres archivos propios, arreglado con prettier --write solo sobre esos tres). bash .agent/verify.sh F-026 --full → 0 (intento 16, las nueve etapas: harness, typecheck, lint, format, test, prisma, build, theme, bundle). En esa misma corrida, /[slug] sale en ● y /[slug]/c/[categorySlug] en 14 rutas pre-renderizadas, verificado en .agent/runs/F-026/016-build.log. bash .agent/verify.sh pending F-026 → sin salida, nada que fichar ni descartar.
- Deja pendiente: nada nuevo. La deuda que queda es la que el plan deja fuera a propósito (conteo por categoría, paginación, breadcrumb de F-025, filtros de F-027, y el arreglo de raíz del contraste 3.33:1 que abre el humano aparte, PP2).
- Siguiente agente sugerido: sdd-tester, para correr los 15 criterios y cerrar tests.md.

### 2026-08-31T04:40:25Z — sdd-tester

- Hizo: verificó los 15 criterios de aceptación de F-026 ejecutando algo real por cada uno (HTTP con curl y código de estado, eventos de sync reales contra /api/internal/sync/catalog y /api/internal/sync/availability con el token de seed-negocio-1, npm run build leído crudo, node scripts/check-bundle-budget.mjs, y un clic real en Chrome vía MCP para V9). Los 15 salieron LISTO. Escribió .agent/specs/F-026/smoke.sh (nuevo, 45 aserciones, autolimpiable) porque F-026 era de los pocos features activos sin guion de runtime.
- Escribió: .agent/specs/F-026/tests.md (veredicto: listo), .agent/specs/F-026/smoke.sh (nuevo).
- Encontró: durante las pruebas, un externalId adivinado mal sobrescribió por accidente "Detergente líquido 1 L" con "Pan de molde" — detectado por conteo (StoreProduct pasó de 15 a 16) y corregido con dos eventos PRODUCT/UPDATE más; la fixture completa de la base de desarrollo quedó restaurada carácter a carácter (28 StoreProduct, 5 LocalCategory, 10 Store, 306 pedidos, 19 CanonicalProduct, verificado contra .agent/sdd.sh start). Dos hallazgos no bloqueantes, documentados en tests.md § Fallos encontrados: (1) el 404 de categoría (y el de /[slug]/pedido/[code] y el 404 global) no lleva ningún <a> real en el HTML crudo — todo el contenido, incluido el enlace de salida, viaja solo en el payload de hidratación de React Flight; sin JavaScript, un comprador que llega ahí no tiene cómo volver. Pre-existente y transversal a toda la app, no lo causa F-026, y ninguno de los 15 criterios lo exige tal como está escrito. (2) dos `npm run build` seguidos sin borrar .next/cache no ven cambios de la base entre uno y otro (el Data Cache de unstable_cache persiste en disco entre invocaciones separadas del proceso de build) — no afecta al criterio 12 ni a ningún otro de los 15, pero es un riesgo operativo si algún día el pipeline de despliegue cachea .next/cache entre builds.
- Verificación: bash .agent/verify.sh F-026 --full → PASA (intento 33, las 9 etapas). bash .agent/verify.sh F-026 --smoke → PASA (intento 29, 44 aserciones sin fallos). bash .agent/verify.sh pending F-026 → vacío.
- Deja pendiente: TP1 (asimetría del umbral "≥2 categorías" entre /[slug] y la vista de categoría, sin dato real para probarla) y TP2 (los dos hallazgos de arriba, ¿abren feature/ficha aparte o se aceptan por escrito?) — las dos van a sdd-architect, ninguna bloquea el veredicto.
- Siguiente agente sugerido: el orquestador, para cerrar el ciclo de F-026 (bash .agent/sdd.sh done F-026) y decidir TP1/TP2 con sdd-architect.

### 2026-08-31T05:40:47Z — sdd-tester

- Hizo: escribió `.agent/specs/F-026/visual.mjs` (F-026 tenía `design.md` en `estado: listo` sin ningún guion visual, y `bash .agent/sdd.sh done F-026` se negaba por eso). Traduce los 11 pasos V1-V11 de `design.md` § Verificación visual contra el servidor real (Playwright, headless), con V10/V11 sembrados y limpiados por sync real (nunca `psql` a secas) y V9 verificado con un clic real (no solo leído) que reemplaza la comprobación manual con la extensión de Chrome de la sesión anterior por una reproducible en cualquier máquina con Bash. V7 se dejó deliberadamente sin traducir porque `smoke.sh` ya lo cubre mejor (curl, cero JavaScript).
- Escribió: `.agent/specs/F-026/visual.mjs` (nuevo), `.agent/specs/F-026/smoke.sh` (corregido: su limpieza por SQL directo no revalidaba la caché del servidor vivo — se le añadió un evento de sync trivial al final, mismo arreglo que se aplicó primero en visual.mjs), `.agent/specs/F-026/tests.md` (dos hallazgos nuevos documentados, sección "Qué cubre el visual y qué no", TP3 nueva, veredicto actualizado).
- Encontró (hallazgo #3, real y estable en 5 corridas): a 360px, el chip "Bebidas" no completa el desplazamiento nativo al enfocarlo con Tab — Chromium solo arrastra a la vista un elemento que está TOTALMENTE fuera de pantalla, y "Bebidas" ya asoma parcialmente antes de enfocarse. `design.md` § V5 exige que el anillo de foco se vea entero en TODOS los chips, incluido ese. R9 prohíbe arreglarlo con JavaScript (sería exactamente el `scrollIntoView` manual que la regla veta), así que no tiene arreglo dentro de las reglas que el propio feature se puso — necesita una decisión de `sdd-designer`/`sdd-implementer` (TP3 en tests.md). Relacionado: el propio `design.md` § V1 asume que el chip que asoma cortado es "Panadería" (la cuarta categoría); medido de verdad es "Bebidas" (la tercera) el que asoma, y "Panadería" queda totalmente oculta — discrepancia de medición, no de criterio.
- Encontró (hallazgo #4, confirma y amplía el #2 del ciclo anterior): la misma foto vieja de la caché de datos de Next (`.next/cache/fetch-cache`, `unstable_cache`) que ya se había visto entre dos `npm run build` también aparece reiniciando `next dev` sin borrar `.next` — descubierto porque `visual.mjs` y `smoke.sh` limpiaban sus fixtures sintéticas con `DELETE` de `psql` directo, que nunca llama a `revalidateTag()`: la base quedaba limpia pero el servidor vivo seguía sirviendo la versión vieja. Se corrigió en los dos guiones (un evento de sync trivial al final de la limpieza) y se confirmó con `rm -rf .next` antes de cada corrida de `--visual`.
- Verificación: `bash .agent/verify.sh F-026 --visual` → sale en **2** (`ESTANCADO`, la misma firma 5 veces seguidas: `VISUAL FAIL V5 — Bebidas`), estable y aislado tras `rm -rf .next` — no es un guion roto, es el hallazgo #3 sin ablandar. Descartado de `pending` con `bash .agent/verify.sh dismiss F-026 'visual:VISUAL FAIL V5 — a 360px, el chip enfocado ("Bebidas") queda dentro de la ventana — esperaba true, obtuve fals' '<motivo, ver journal.tsv>'` porque es un hallazgo real que ya quedó documentado y escalado (TP3), no un descuido que dar por bueno. `bash .agent/verify.sh F-026 --full` → PASA (intento 49, las 9 etapas). `bash .agent/verify.sh pending F-026` → vacío. Base de desarrollo restaurada a la misma foto de siempre (28 StoreProduct, 5 LocalCategory, 10 Store, 19 CanonicalProduct) tras cada corrida de `--visual`/`--smoke`.
- Deja pendiente: TP3 (el hallazgo #3 de arriba) y TP1/TP2 del ciclo anterior, todas sin resolver — van a `sdd-designer`/`sdd-architect`. El veredicto de los 15 criterios de aceptación sigue en `listo` (design.md § V1-V11 es una capa de verificación distinta de `.agent/features.json`, y ninguno de los 15 exige lo que el hallazgo #3 rompe), pero `--visual` queda deliberadamente en rojo (código 2) hasta que alguien decida sobre TP3.
- Siguiente agente sugerido: el orquestador, para llevar TP1/TP2/TP3 a `sdd-designer`/`sdd-architect` y decidir si F-026 se cierra con `--visual` en rojo documentado o si antes hace falta un ajuste de diseño.
