---
feature: F-041
actualizado: 2026-09-09T03:58:39Z
branch: feature/f041-zone-shipping-contract
ciclo: 1
---

## Estado actual

**Construido y verificado, incluido el paso 17; abierto a propósito, esperando
a cuadrecaja.** Los 17 pasos del plan firmado están hechos: el índice de 184
filas commiteado con su procedencia y su informe de la unión, la precedencia
como función pura, el enum `ZONE_BASED`, la entidad `ZONE_TARIFF` con su
handler, la migración aplicada contra la base real, la siembra idempotente,
las siete pruebas del paso 16, y —del paso 17, tras el veredicto de
cuadrecaja del 2026-09-09 sobre el borrador («OK a la forma, condicionado a
nueve puntos»)— el hash del bloque JSON del vector publicado y comprobado en
`precedence.test.ts`, los cuatro códigos de error nuevos clasificados
permanente/reintentable, la respuesta escrita al P7 y los cinco menores.
`bash .agent/verify.sh F-041 --full` sale **0** en las nueve etapas —comprobado
por el orquestador y no solo por los agentes— y `npm test` da **1658** pruebas
en 153 ficheros (el +1 sobre el paso 16 es la prueba nueva del hash). `tests.md`
cierra con `veredicto: listo` y los 17 criterios (los 14 de `features.json` más
C15-C17) tienen casilla y comando; el paso 17 no abre criterios nuevos —cae
dentro de C13— así que no se re-marca nada en la sección de abajo.

`"passes"` sigue en **`false`** y el fichero de progreso sigue vivo a propósito:
ver § «Bloqueado por».

## Criterios cubiertos

Los `acceptance_criteria` del feature —**todos**, en el mismo orden que
`features.json`— con lo que se ejecutó para verificar cada uno. Un criterio sin
comando ejecutado no está cubierto, y `sdd.sh done` cuenta las casillas marcadas
contra las del feature: no cuadra, no cierra.

Los catorce de `features.json`, más C15-C17 de `spec.md`, **todos cubiertos
ahora** por el paso 16 de `sdd-tester` (siete ficheros de test nuevos más dos
bloques añadidos a ficheros existentes). Detalle completo, con comando y
salida por criterio, en `.agent/specs/F-041/tests.md`.

- [x] C1 «Los SIETE casos del vector pasan, leídos del propio contrato» →
      `src/features/zones/precedence.test.ts` lee el bloque JSON de
      `docs/sync-contract.md` y ejecuta `resolveZoneTariff` sobre los 13
      casos que declara: `npx vitest run src/features/zones/precedence.test.ts`
      → 15 passed (13 casos + el conteo ≥10 + el conteo ejecutado=declarado)
- [x] C2 «Un ZONE_TARIFF válido responde 207 processed y la fila se guarda» →
      `zoneTariff.db.test.ts` § "C2 (E1)", POST real contra Postgres, fila
      leída con `prisma.zoneTariff.findUnique`
- [x] C3 «NOT_SERVED/INHERIT con importe, y FEE sin importe, dan 400» →
      `schemas.test.ts` § "zoneTariffPayloadSchema…" (12 tests, incluida la
      trampa del discriminante de Zod) + `zoneTariff.db.test.ts` § "C3, the
      Zod discriminator quirk" (POST real, `400 INVALID_BATCH`)
- [x] C4 «DELETE responde failed[] y no borra nada» → `zoneTariff.db.test.ts`
      § "C4 (E4) + E5", contando filas antes/después con `countTariffRows`
- [x] C5 «updatedAt <= no pisa el importe vigente» → `zoneTariff.db.test.ts`
      § "C5 (E3)" (POST real, `<` y `==`) + `zoneTariff.test.ts` (mockeado,
      mismos dos casos aislados del handler)
- [x] C6 «zona desconocida responde 400 con su código» →
      `zoneTariff.db.test.ts` § "C6 (E9)", `issues[].message ===
ZONE_TARIFF_ZONE_UNKNOWN`, cero `SyncEvent`, cero filas
- [x] C7 «INHERIT de provincia se acepta y resuelve no servida» →
      `zoneTariff.db.test.ts` § "E12" (la aceptación, POST real) +
      `tariffs.db.test.ts` (la resolución, `resolveStoreZoneTariff` contra
      filas reales)
- [x] C8 «Borrar STORE con DELETE se lleva sus tarifas» →
      `zoneTariff.db.test.ts` § "C8" (2 tests: el camino del sync —contando
      antes/después, verificando que la fila `Store` queda SUSPENDED, no
      borrada— y la FK `onDelete: Cascade` real, borrando la fila `Store` de
      verdad)
- [x] C9 «STORE con zoneCode desconocido falla ESE evento» →
      `zoneTariff.db.test.ts` § "C9" (2 tests: desconocido falla con
      `STORE_ZONE_UNKNOWN` y ni siquiera aplica un `phone` que viajaba bien;
      válido se guarda y se lee)
- [x] C10 «ZONE_BASED se guarda y se lee; migración aditiva» →
      `zoneTariff.db.test.ts` § "C10" (POST real, `deliveryEnabled: true` +
      `deliveryFeeMode: "ZONE_BASED"` sin `deliveryFee`, `processed`, columna
      leída de vuelta) + lo ya verificado por `sdd-implementer`: `npx prisma
migrate deploy` aplicó `20260909012319_zone_shipping`; `git diff main
--stat -- prisma/migrations` solo el fichero nuevo
- [x] C11 «Catálogo sembrado, versión anotada, sembrar dos veces es
      idempotente» → `catalogSeed.db.test.ts` (5 tests): dos llamadas
      seguidas a `seedZoneCatalog(prisma)` dejan 184/16/168 las dos veces,
      misma `version`/`sha256`, una sola fila en `ZoneCatalogVersion`
- [x] C12 «Aplicar ZONE_TARIFF expira las páginas cacheadas» →
      `zoneTariff.db.test.ts` § "C12/E18" (5 tests: una sola tienda →
      exactamente 2 tags; 20 tarifas de la misma tienda en un lote → 2 tags,
      no 40; `stale`/`failed`/`skipped` → cero invalidaciones)
- [x] C13 «v13 con entidad, precedencia con letra, códigos, vector JSON
      calculado ejecutando» → el propio `precedence.test.ts` ejecuta el
      bloque del contrato (ver C1); heredado de `sdd-implementer`: `head -3
docs/sync-contract.md | grep -c '13'` → `1`; línea fechada 2026-09-09
      en `.agent/solicitudes.md` § S-007. **Publicar** sigue pendiente del
      visto bueno de cuadrecaja
- [x] C14 «bash .agent/verify.sh F-041 --full termina con código 0» → PASA
      (harness·typecheck·lint·format·test·prisma·build·theme·bundle), **con
      los siete tests del paso 16 dentro**: `npm test` → 1657 passed (153
      test files), 94 más que el baseline de 1563/146
- [x] C15 «ZONE_BASED con deliveryFee residual no cobra en el pedido» →
      `createOrder.test.ts` § "F-041 — ZONE_BASED" (2 tests, mockeado):
      `DELIVERY` con un `deliveryFee` residual de 500.00 en la columna
      degrada a `PICKUP` con `0.00`, nunca 500.00 ni un cobro silencioso
- [x] C16 «Integridad del índice: 184/16/168, sin duplicados, 183 ids, sha256»
      → `catalog.test.ts` (14 tests): recuentos, cero duplicados, 183
      `osmRelationId` distintos (el repetido es `1854614`, `40`/`40.01`), el
      sha256 del artefacto contra el publicado en `docs/sync-contract.md`
      (leído del propio documento, no transcrito)
- [x] C17 «Informe de la unión commiteado, cita la errata, procedencia con la
      URL del Archive» → heredado de `sdd-implementer` (documental, sin
      comportamiento nuevo que ejecutar en este ciclo): `grep -c "34.0"
src/features/zones/zone-index.join-report.md` → `12`; `grep -c
"web.archive.org/web/20110125204824id_"
src/features/zones/zone-index.provenance.md` → `1`

## Decisiones tomadas

Las cuatro primeras las resolvió el humano el 2026-09-08, antes del primer
especialista:

- **El alcance del catálogo en este feature es SOLO EL ÍNDICE** (código → nombre,
  16 divisiones de primer nivel y 168 municipios, sembrado con su versión e
  idempotente). **La geometría es de F-042**, que es quien la consume para el
  mapa. Los dos artefactos ya tenían versiones separadas por diseño; esto decide
  en qué feature nace cada uno. Consecuencia: aquí NO se instala herramienta de
  simplificación topológica y NO se genera ningún polígono.
- **Los nueve municipios de Santiago de Cuba se siembran como `34.01`–`34.09`.**
  El PDF de ONEI los imprime con el prefijo de Holguín (`32.01`–`32.09`, cuando
  `32.01`–`32.14` ya son de Holguín); es una errata de la fuente, y su propio
  encabezado dice «PROVINCIA: 34 SANTIAGO DE CUBA». La errata va **citada** en la
  procedencia y en el informe de la unión, para que no parezca un número puesto
  por nosotros.
- **El borrador de la v13 sale hacia cuadrecaja al firmar el plan**, no al final:
  ellos revisan mientras se implementa. Publicar la versión espera su visto
  bueno, que es lo que pide el criterio 13.
- **Se implementan los DIEZ casos del vector**, no siete. El criterio 1 dice
  «los SIETE casos», y con los diez en el contrato y los diez en verde queda
  cumplido de sobra: no se modifica ningún criterio (regla 3 intacta) y no se
  abre feature correctivo. La cifra corta queda anotada en `spec.md`.

Las cuatro siguientes las resolvió el humano el 2026-09-08 **después** de leer
`spec.md`, y en las cuatro eligió lo que la spec recomendaba. Están escritas al
pie de su pregunta en `spec.md` § «Huecos y preguntas al humano»:

- **SP2 · La ventana entre F-041 y F-042: una tienda `ZONE_BASED` NO ofrece
  domicilio** — degrada a recogida en silencio, como ya hace F-010 cuando no hay
  con qué cerrarlo. Pero **su configuración sí se acepta y se guarda**, para que
  el POS pueda cargar su tarifario desde el primer día: de ahí que haya que
  desacoplar `isDeliveryConfigInconsistent` (I4). Cobrar el `deliveryFee`
  residual queda descartado — es la única opción que no cobra un importe que
  nadie fijó para esa zona.
- **SP1 · Los diez casos del vector se recomputan ejecutando y se recruzan** —
  se publican en el borrador de la v13 con las tres guardas y se les pide su
  lista al enviarlo. Si difieren, se publica **la unión**: ningún caso se quita
  para que la cuenta dé diez.
- **SP3 · `STORE` pasa a proveer clave de dependencia para `ZONE_TARIFF`** — un
  tarifario cuyo `STORE` falló en el mismo lote vuelve como
  `DEPENDENCY_FAILED_IN_BATCH` en `failed[]`, y el cambio entra en la v13. Sin
  ello sale `skipped_not_published`, que viaja dentro de `ok`, y el POS cree
  guardada una tarifa que no se guardó.
- **SP4 · El `code` viaja con punto: `34.01`** — verbatim del Codificador de
  ONEI. Normalizar al leer queda descartado.

Lo ya cerrado antes de abrir el feature, que no se re-decide y está en
`.agent/specs/propuestas/zonas-de-envio.md`: `rule` con tres valores
(`FEE`/`NOT_SERVED`/`INHERIT`) y no dos banderas; `ZONE_TARIFF` **sin** `DELETE`;
la precedencia municipio → provincia → no servida; el nivel de cada zona como
campo declarado y no deducido del código; un código retirado que se lee y no se
ofrece; y las tres guardas del vector (`FEE` sin importe no decide, `0` es envío
gratis comprobado contra `null` y no contra un falsy, negativo se rechaza y no
decide).

## Problemas resueltos en este ciclo

Qué falló en la verificación y cómo se arregló. Uno por línea, con la firma que
imprimió `verify.sh` y —si dio lección— la ficha de `.agent/playbook/` donde
quedó escrita. `sdd.sh done` no cierra el feature mientras quede un fallo que no
esté ni fichado ni descartado.

<!-- Ejemplo del formato; bórralo al escribir los de verdad:
- `test:TypeError Cannot read properties of undefined` → el fixture no traía el
  campo. Descuido, descartado: no daba lección a nadie.
- `build:Invalid revalidate value` → constante importada en un segment config.
  Ficha: .agent/playbook/revalidate-no-literal.md. Ya estaba: la leí y la apliqué.
-->

- `test:isDeliveryOffered: unhandled mode undefined` (`createOrder.test.ts`) →
  el fixture compartido `store` nunca declaraba `deliveryFeeMode`, y confiaba
  en que la versión vieja de `isDeliveryOffered` cayera en su rama por
  defecto con cualquier valor. El `switch` exhaustivo nuevo (I4) lo señaló
  correctamente. Descartado, no ficha: es exactamente el comportamiento que
  I4 pedía cambiar, no un fallo del entorno — se arregló añadiendo
  `deliveryFeeMode: "FLAT_RATE"` al fixture.
- `test:Cannot read properties of undefined (reading 'deleteMany')`
  (`store.test.ts`) → el mock de `@/lib/prisma` no incluía `zoneTariff`.
  Descartado, no ficha: consecuencia directa y prevista de R22(b) (el `DELETE`
  aplicado se lleva las filas del tarifario) — se añadió el mock.
- `test:expected { provides: 'STORE:...' } to deeply equal { provides: null }`
  (`dependencies.test.ts`) → el test afirmaba el rol viejo de `STORE`. SP3/R23
  decide justo lo contrario. Descartado, no ficha: el test se reescribió para
  su nuevo rol.
- `harness:'handlers/store.ts' does not exist` → ruta abreviada citada en la
  propia bitácora de este archivo (línea de `sdd-architect`, no mía). Ficha ya
  existente: `.agent/playbook/check-harness-falso-positivo-ruta-abreviada.md`
  — se corrigió a la ruta completa en el mismo sitio.

Ninguno de los cuatro dio lección nueva: los tres primeros son consecuencia
prevista y documentada de decisiones del propio plan (I4, R22(b), SP3), y el
cuarto ya tenía ficha.

## Bloqueado por

**Cuadrecaja, y la acción de arranque es del humano: enviarles el borrador de la
v13.** Decisión del humano el 2026-09-09, después de que el orquestador señalara
que el criterio 13 pide la v13 «coordinada con cuadrecaja **antes** de
publicarla» y que esa mitad **no se puede verificar ejecutando nada**: depende de
que ellos contesten. Firmar ahora habría declarado terminado algo que todavía
puede moverse, porque por la regla de la unión (SP1) su lista del vector puede
traer casos que la nuestra no tiene, y entonces cambian el contrato y su bloque
JSON.

`docs/sync-contract.md` está en «Versión 13 · **BORRADOR, sin publicar**» con la
advertencia de no emitir nada todavía. Eso es correcto y no hay que tocarlo hasta
que ellos acusen.

## Próximo paso concreto

**Enviar a cuadrecaja el borrador de la v13** —acción del humano— con las dos
cosas que decidió en AP2: la ruta de `docs/sync-contract.md` citada, y
`src/features/zones/zone-index.json` **adjunto**, versión `1.0.0`, sha256
`9bb89dd3564b0b202f975160f98be449efdc3c2b3d8594df6a7ec8009853b019`, para que lo
commiteen en su repo y comprueben el hash. El documento ya les pide su lista del
vector.

Cuando contesten, en este orden: (1) si su vector trae casos que no están, se
añaden por la regla de la unión —ninguno se quita para que la cuenta dé trece— y
se vuelve a correr `npm test`, que lee el bloque del contrato y no una copia;
(2) se quita la marca de BORRADOR y se publica la v13; (3) se anota la línea
fechada en la fila de S-007 de `.agent/solicitudes.md`; (4) se le propone al
humano poner `"passes": true`; (5) `bash .agent/sdd.sh done F-041`.

El orden importa y el script se niega a invertirlo.

## Notas para quien retome

- **La migración NO se hace con `prisma migrate dev`.** Esta base Postgres se
  comparte entre worktrees y `migrate dev` se niega a avanzar por checksum drift
  y ofrece resetear el esquema. Fichas:
  `.agent/playbook/prisma-migrate-dev-checksum-drift-bd-compartida.md` (genera el
  DDL con `prisma migrate diff --from-config-datasource --to-schema
prisma/schema.prisma --script`, carpeta a mano, `prisma migrate deploy`) y
  `.agent/playbook/prisma-migrate-dev-borra-indices-gin-no-declarados.md` (quita
  los `DROP INDEX` de los cinco índices GIN/parciales no declarados).
- **La aditividad del criterio 10 no se comprueba con `migrate diff --exit-code`**,
  que nunca da cero en este repo por esos mismos índices. Ficha:
  `.agent/playbook/prisma-migrate-diff-nunca-da-cero-por-indices-no-declarados.md`
  → `git diff main --stat -- prisma/migrations`.
- La lista DPA vino del Internet Archive y la URL exacta con su marca de tiempo
  está en la propuesta, § «El DPA, recuperado del Internet Archive». El PDF no
  está commiteado.

## Bitácora

Append-only, en orden cronológico. Una entrada por agente que trabaja, escrita
por él mismo al terminar con `bash .agent/sdd.sh log <id> <agente>`.

El estado de los artefactos no se anota aquí: lo deriva `sdd.sh status` del
frontmatter de cada uno.

### 2026-09-08T19:09:26Z — sdd-spec

Escrita `.agent/specs/F-041/spec.md` sobre la plantilla, en `estado: listo`
(1211 líneas): 22 escenarios, 29 reglas, 16 casos límite, la forma del payload de
`ZONE_TARIFF`, los modelos, los tres códigos de error nuevos, el formato del
vector con sus trece casos (diez de precedencia más las tres guardas) y el
índice de lo que la v13 tiene que llevar. Los 14 criterios traducidos a algo
ejecutable, más tres propuestos `[nuevo]` (C15 el cobro en la ventana previa a
F-042, C16 la integridad del índice, C17 la errata citada). Recogidos los
acuerdos que se cerraron DESPUÉS de escribirse los criterios y que ninguno
menciona: índice versionado con nivel declarado, provincia, id y nombre de OSM y
marca de retirado; código retirado que se lee y no se ofrece; procedencia junto a
los bytes; las tres guardas del vector; el camino completo por caso; y el test
que lee el JSON del propio contrato.

Doce incongruencias, las cuatro que cambian el trabajo: I1, el criterio 8 y la
R7 de la propuesta dan por hecho que un `STORE` con `operation: DELETE` borra la
fila `Store` y NO la borra (la suspende; nada en el repo ejecuta
`store.delete`), así que una cascada por clave ajena nunca se dispararía —R22 lo
cumple con las dos mitades—. I4, añadir `ZONE_BASED` al enum pone `typecheck` en
rojo (`DeliveryFeeModeName` es una unión escrita a mano) y, sin desacoplar
`isDeliveryConfigInconsistent` de `isDeliveryOffered`, haría imposible configurar
una tienda `ZONE_BASED` con domicilio: mataría el criterio 10 en su caso
realista. I3, la composición exacta de los diez casos cruzados el 2026-09-06 no
está escrita en ningún fichero de este repo, solo descrita. I5, `Store.zoneCode`
mueve de 31 a 32 tres números clavados a mano (`fieldOwnership.test.ts` y dos
sitios del contrato). Las demás: I2 (siete contra diez, anotado sin tocar el
criterio), I6/I7 (la asimetría 400 contra failed[], y que el 400 solo es posible
porque el índice son bytes commiteados), I8 (producción no corre
`prisma/seed.ts`: hace falta punto de entrada propio y línea en
`docs/despliegue.md`), I9 (la Isla obliga a que el id de OSM no sea único: 184
filas, 183 relaciones), I10 (0028 se reabre, 0011 no), I11 y I12.

Cuatro preguntas, todas NO BLOQUEANTES y con su defecto ya escrito como regla:
SP1 la composición de los diez casos del vector; SP2 la semántica de `ZONE_BASED`
en la ventana anterior a F-042; SP3 la cascada intra-lote `STORE → ZONE_TARIFF`;
SP4 el `code` con punto o sin él.

Siguiente: `sdd-architect`. La spec deja decidido el QUÉ y le deja tres cosas
suyas: nombres de modelos, columnas y enums; dónde viven los bytes del índice
(con la restricción de que el schema del sobre tiene que importarlo sin base y
que no entre en el bundle de cliente); y cómo se normaliza el importe a dos
decimales.

### 2026-09-08T21:41:43Z — sdd-architect

Escrita `.agent/specs/F-041/architecture.md` sobre la plantilla, en
`estado: listo` (1130 líneas), con `bash .agent/verify.sh F-041` en PASA
(typecheck·lint·format·test) y `npm run check:harness` en verde tras arreglar
dos rutas abreviadas —`src/features/sync/server/handlers/store.ts`— que daban «does not exist» sobre un
archivo que sí existe (la mitad simétrica de la ficha de check-harness).

LAS TRES COSAS QUE LA SPEC RESERVABA, CERRADAS. (1) Nombres: modelos `Zone`,
`ZoneTariff` (clave primaria compuesta `@@id([storeId, zoneCode])`) y
`ZoneCatalogVersion`; enums `ZoneLevel { FIRST_LEVEL, MUNICIPALITY }` y
`ZoneTariffRule { FEE, NOT_SERVED, INHERIT }`; columna `Store.zoneCode`;
`retiredAt DateTime?` calcado de `Slug.retiredAt`. (2) Los bytes del índice: un
JSON commiteado en src/features/zones/zone-index.json (por crear) importado
ESTÁTICAMENTE por src/features/zones/catalog.ts (por crear) —sin `fs`, sin
Prisma, sin React—, que es lo que hace alcanzable el `400` del criterio 6 (I7);
descartadas una carpeta `data/` en la raíz, `src/data/` y leer el fichero en
tiempo de petición. Y el hallazgo que va con eso: la etapa `bundle` NO es la
garantía de que no entre en cliente (serían ~7 KB gzip sobre un techo de 193 con
la peor página en 182,1, así que pasaría), de modo que la garantía son una
entrada en `no-restricted-imports` de `eslint.config.mjs` y
src/features/zones/boundaries.test.ts (por crear) con la lista blanca de
importadores —necesaria porque el checkout vive en
`src/features/cart/components/CheckoutForm.tsx`, que la regla de ESLint no
cubre—. Además el artefacto entra en `.prettierignore`: Prettier le cambiaría
los bytes y con ellos el sha256 publicado. (3) El importe: `src/lib/money.ts`
gana UNA exportación, `toDecimalString(MoneyInput): string`, que reutiliza las
`parseToMinor`/`minorToString` que ya están escritas; descartados
`money(fee, "CUP").amount`, `toFixed(2)` y una segunda aritmética en el módulo
de zonas.

DECISIONES DE FORMA. Dominio nuevo `src/features/zones/` con tres piezas de
vidas separadas: índice (bytes), precedencia (función pura, sin catálogo y sin
base, para que F-042 la pueda importar en cliente) y `server/` (siembra y lector
mínimo, lo único que toca Prisma). El sync se toca en los cinco sitios que ya
son costura. I4 se cierra partiendo la pregunta en dos funciones con `switch`
exhaustivo y `never`: `hasSomethingToChargeDeliveryWith` (la del sync, que en
`ZONE_BASED` dice SÍ y acepta la configuración) e `isDeliveryOffered` (la del
comprador, que hasta F-042 dice NO); `DeliveryFeeModeName` sale del enum
generado con `import type`, cero bytes en cliente. El payload de `ZONE_TARIFF`
es una `z.discriminatedUnion` sobre `rule` con `z.never().optional()` para
prohibir el importe —precedente literal de `barcode`—, así que `rule: "FEE"`
IMPLICA `deliveryFee: number` en TypeScript.

ESCALABILIDAD, CON NÚMEROS. Validar un `zoneCode` cuesta una búsqueda en un
`Map` de 184 entradas y CERO consultas; el handler son 2 llamadas de Prisma por
evento (~1000 round-trips en un lote de 500, 2-4 s, del mismo orden que los
~1500 de un lote de 500 `PRODUCT` hoy); la resolución es UNA consulta y ≤2 filas
por la clave primaria, porque el nivel y la provincia salen del índice y no de
la tabla; la siembra es UN statement de 1288 parámetros
(`INSERT … ON CONFLICT DO UPDATE`, que es lo que `createMany({skipDuplicates})`
no sabe hacer); +0 KB de datos y <0,2 KB de código en cliente, así que NO hay
que subir `BUDGET_KB`. Lo primero que se rompe: el `findUnique` de la sucursal
por evento cuando llegan las 168 tarifas juntas; la mitigación obvia —un memo
por lote— queda DESCARTADA por escrito, porque el slug canónico depende de
`brandBranchCount` y cachearlo invalidaría la etiqueta equivocada (la ficha
`revalida-solo-lo-que-se-escribe-no-lo-que-cambia-de-significado`).

UNA INCONGRUENCIA NUEVA, I13, que la spec no vio: dos tests usan HOY
`ZONE_TARIFF` como ejemplo de «entidad que el contrato no define»
(`src/features/sync/schemas.test.ts:255-263` y
`src/features/sync/server/handlers/business.db.test.ts:189-225`), y los dos
SEGUIRÁN PASANDO cuando la entidad exista —el primero porque `payload: {}` falla
igual, el segundo porque su payload no lleva `storeId` ni `zoneCode`—, así que
quedarían verdes con el nombre mintiendo y sin comprobar lo que dicen. Hay que
reapuntarlos a una entidad que de verdad no exista, en el mismo commit. Y I5 son
CINCO números clavados, no tres: dos asertos de `fieldOwnership.test.ts:141-145`
más `docs/sync-contract.md:517`, `:1198` («54 filas» → 55) y `:1203`.

PENDIENTE Y DE QUIÉN. Dos ADR que este ciclo NO escribe (el prompt prohíbe tocar
`docs/`) y que van al plan con su número reservado: 0032 «El catálogo de zonas
son bytes commiteados y la base es su espejo» y 0033 «Con qué cobrar el
domicilio y ofrecerlo son dos preguntas distintas», que estrecha el invariante
(e) de la 0028 y por eso no puede ser una nota. La 0011 no se reabre. Y ocho
ficheros de fuera de `src/` que el plan tiene que ordenar como pasos propios:
`package.json` (dos guiones), `.prettierignore`, `eslint.config.mjs`, la línea
de `AGENTS.md` § Cosas que muerden que pasa de cuatro entidades a cinco (I11),
`docs/despliegue.md` § 1, la v13 de `docs/sync-contract.md`,
`.agent/solicitudes.md` y `FORBIDDEN_WRITE_COLUMNS` de
`src/features/admin/server/boundaries.test.ts`.

CUATRO PREGUNTAS AL HUMANO, todas con recomendación y ninguna bloquea escribir
código, pero las cuatro bloquean la firma del plan: AP1 los seis veredictos del
camino del vector en inglés (recomendado) o verbatim de la spec en castellano;
AP2 cómo recibe cuadrecaja los bytes del índice (ruta citada en el contrato más
el adjunto del borrador, recomendado; no un endpoint); AP3 la siembra en
producción a mano una vez por entorno con su línea en `docs/despliegue.md`
(recomendado) o enganchada al despliegue; AP4 si estrechar la 0028 (e) es una
ADR nueva (recomendado) o una nota fechada.

Siguiente: el orquestador destila esto en `.agent/specs/F-041/plan.md` y lo firma
el humano. `sdd-designer` no tiene trabajo aquí —este feature no tiene pantalla,
la del tarifario es de cuadrecaja—, así que tras la firma va `sdd-implementer`.

### 2026-09-09T01:03:54Z — humano

- Hizo: aprobó el plan
- Escribió: .agent/specs/F-041/plan.md (aprobado: sí)
- Dijo: «apruebo»
- Siguiente agente sugerido: sdd-implementer (el plan ya es ejecutable)

### 2026-09-09T01:50:07Z — sdd-implementer

Construidos los pasos 1-15 del plan firmado. El paso 16 (siete tests nuevos) es
de sdd-tester y no se escribió aquí.

PASO 1 — el índice. Descargado el PDF del Codificador desde el Internet
Archive (sha256 verificado), extraído su texto inflando cada stream con zlib y
reconstruyendo líneas por los operadores Tm/Td/T* (no hay pdftotext en esta
máquina), incluido un glyph de un tipo de letra de símbolos (hex 00b1, el
guion de «Songo - La Maya»). Consulta viva a Overpass el 2026-09-09: 183
relaciones (16 admin_level=4, 167 admin_level=6), unidas por nombre
normalizado con 4 emparejamientos manuales (San Luis × 2 desambiguado por
wikipedia, La Habana del Este/Habana del Este, Carlos Manuel de
Céspedes/Céspedes). 184 filas exactas, 183 osmRelationId distintos (el
repetido es la Isla de la Juventud, 40 y 40.01). Santiago de Cuba sembrada
34.01-34.09 con la errata 32.xx citada en procedencia y en el informe de la
unión. zone-index.json entra en .prettierignore en el mismo commit.

PASOS 2-5 — money.toDecimalString, catalog.ts (lector sin fs/Prisma + regla
ESLint no-restricted-imports), precedence.ts (función pura, 3 guardas de
R27), compute-zone-vector.ts (13 casos ejecutados, cotejados a mano contra la
tabla de spec.md: los 13 coinciden).

PASO 6 — schema: ZONE_BASED, ZONE_TARIFF, ZoneLevel, ZoneTariffRule, Zone,
ZoneCatalogVersion, ZoneTariff, Store.zoneCode. Migración generada con
`prisma migrate diff --from-config-datasource --to-schema` (nunca migrate
dev), carpeta a mano, aplicada con `migrate deploy`. Sin DROP INDEX que quitar
(diff limpio). Aditividad: git diff main --stat -- prisma/migrations solo
muestra el fichero nuevo (untracked); git diff main -- prisma/schema.prisma
son 106 inserciones, cero borrados.

PASOS 7-8 — Store 31→32 y 54→55 filas en fieldOwnership.test.ts y
sync-contract.md (tres sitios + la fila zoneCode nueva en la tabla).
seedZoneCatalog en un INSERT...ON CONFLICT (Prisma.sql/Prisma.join, 1288
parámetros), llamado desde prisma/seed.ts y desde scripts/seed-zone-catalog.ts
(producción). Verificado sembrando dos veces: 184/16/168 las dos veces, misma
version/sha256, una sola fila en ZoneCatalogVersion. Línea en
docs/despliegue.md § 1.

PASO 9 — I4 resuelto: DeliveryFeeModeName del enum generado;
hasSomethingToChargeDeliveryWith (la pregunta del sync, SÍ en ZONE_BASED) e
isDeliveryOffered (la del comprador, NO hasta F-042) como dos funciones con
switch exhaustivo; deliveryFeeForNewOrder lanza en ZONE_BASED+DELIVERY
(inalcanzable por construcción). Desviación: src/features/orders/types.ts
tenía una TERCERA unión hand-rolled (QuoteStore.deliveryFeeMode) que
typecheck señaló — corregida con el mismo patrón.

PASO 10 — zoneTariffPayloadSchema (discriminatedUnion por rule, z.never()
para prohibir importe en NOT_SERVED/INHERIT), séptima rama del sobre,
zoneCode en storePayloadSchema (omitir no es apagar), 4 constantes de error
nuevas.

PASO 11 — handleZoneTariff: DELETE rechazado primero (cero consultas), la
sucursal en una consulta con su fila de tarifario anidada, guarda >= (STALE),
upsert, touchedStoreSlug. Séptimo case en applyEvent. STORE pasa a proveer
STORE:<storeId>, ZONE_TARIFF lo requiere (SP3/R23).

PASO 12 — zoneCode en STORE_CONFIG_KEYS; assertZoneKnown en las tres
posiciones que ya usan assertDeliveryConsistent; zoneTariff.deleteMany en el
camino aplicado del DELETE de STORE (R22(b), la fila hoy se suspende, nunca
se borra — sin esto la cascada FK nunca dispararía en producción).

PASO 13 — resolveStoreZoneTariff: findZone (índice, cero consultas) + ≤2
filas por PK + resolveZoneTariff (función pura).

PASO 14 — los dos tests que usaban ZONE_TARIFF como "entidad que el contrato
no define" (I13) reapuntados a ORDER. zoneCode en FORBIDDEN_WRITE_COLUMNS.

PASO 15 — v13 completa: header a v13 (BORRADOR, sin publicar), sección
"Cambios respecto a la v12.2" con la entidad ZONE_TARIFF, zoneCode en STORE,
la precedencia con letra, el vector JSON (el bloque es literalmente la salida
de compute-zone-vector.ts), la cascada STORE→ZONE_TARIFF, la versión del
catálogo (1.0.0, su sha256, la ruta). Tres códigos en el vocabulario de
errores. "Cambios requeridos en cuadrecaja" con la sección "De la v13". ADR
0032 (el catálogo como bytes commiteados) y ADR 0033 (dos preguntas de
domicilio, estrecha 0028(e)) nuevas; 0028 gana la línea "superada en (e)".
AGENTS.md: la guarda que rechaza pasa de 4 a 5 entidades, más la cascada.
Línea fechada 2026-09-09 en solicitudes.md § S-007.

ENCARGO DE ORDEN cumplido: en cuanto terminó el paso 5 se escribió el
contenido del borrador de la v13 que ya se podía escribir (entidad,
precedencia con letra, códigos, vector JSON, cascada, versión del índice con
sha256 y ruta) — reportado en el informe final para que el orquestador lo
reenvíe a cuadrecaja sin esperar al resto.

CUATRO DESVIACIONES DEL ÁRBOL, ninguna del plan firmado: src/features/orders/types.ts
(tercera unión hand-rolled que typecheck señaló), createOrder.test.ts (fixture
sin deliveryFeeMode explícito), dependencies.test.ts (el test de STORE
afirmaba el rol viejo, SP3 lo cambia a propósito), store.test.ts (mock de
prisma.zoneTariff faltante). Las cuatro documentadas en impl.md § Desviaciones
con su porqué; ninguna dio lección nueva de playbook (las cuatro son
consecuencia prevista de I4/R22(b)/SP3).

bash .agent/verify.sh F-041 --full → PASA
(harness·typecheck·lint·format·test·prisma·build·theme·bundle). npm test:
1563 passed, 146 test files (server + db). check:harness arreglado por una
ruta abreviada preexistente en la bitácora del propio F-041.md (ficha
check-harness-falso-positivo-ruta-abreviada, ya conocida).

Siguiente: sdd-tester, los siete tests del paso 16. .agent/progress/F-041.md
§ "Criterios cubiertos" marca qué se pudo verificar sin ellos (C8, C10, C11,
C13, C14, C16, C17) y qué queda para sus test files (C1-C7, C9, C12, C15).

### 2026-09-09T02:15:38Z — sdd-tester

Escrito `.agent/specs/F-041/tests.md` (`veredicto: listo`) y las siete
pruebas del paso 16, más dos bloques añadidos a ficheros ya existentes que
la arquitectura no había asignado a ningún fichero (C9, STORE con zoneCode
desconocido, y C10, ZONE_BASED contra la base real — ninguno tenía cobertura
automatizada antes de este ciclo, así que se cerraron dentro de
`zoneTariff.db.test.ts`, que ya traía el helper de eventos STORE para C8/E19).

SIETE FICHEROS NUEVOS: `src/features/zones/precedence.test.ts` (15 tests —
lee el bloque JSON de `docs/sync-contract.md` con un parser propio, ejecuta
`resolveZoneTariff` sobre los 13 casos que declara y afirma que el número
ejecutado es exactamente el declarado, ≥10 — C1/E20); `catalog.test.ts` (14 —
recuentos 184/16/168, cero duplicados, 183 `osmRelationId` distintos con el
repetido `1854614` nombrado, y el sha256 del artefacto contra el publicado en
el contrato, leído del propio documento — C16/E22); `boundaries.test.ts` (2 —
lista blanca de importadores de `@/features/zones/catalog`, resuelta
siguiendo cada especificador relativo Y el alias `@/` contra los dos ficheros
reales, no por substring — la garantía que `check:bundle` no daría, ~7 KB
gzip sobre 193); `zoneTariff.test.ts` (13, Prisma mockeado — el orden R20 de
`DELETE` antes de cualquier consulta, R24 el `SKIPPED` de sucursal ajena, y
las dos mitades de R21 incluida la igualdad); `zoneTariff.db.test.ts` (20,
Postgres real vía el `POST` real — C2, C3 el discriminante de Zod, C4, C5,
C6, C7 la mitad de aceptación, C8 las dos formas de la cascada, C9, C10,
C12 con el conteo de invalidaciones, y E19 la dependencia intra-lote);
`tariffs.db.test.ts` (7 — filas reales entrando en la función pura, C7 la
mitad de resolución); `catalogSeed.db.test.ts` (5 — sembrar dos veces, C11).

DOS BLOQUES AÑADIDOS: `schemas.test.ts` § zoneTariffPayloadSchema (12 tests,
C3 al nivel del schema, incluida la trampa del discriminante de Zod
documentada expresamente); `createOrder.test.ts` § F-041 ZONE_BASED (2, C15 —
un `deliveryFee` residual de 500.00 no se cobra, el pedido degrada a PICKUP
con 0.00).

LAS TRAMPAS DEL ENCARGO, verificadas una por una: R27 las tres guardas (FEE
sin importe no decide y cae al escalón de arriba — G1 del vector; 0 SÍ decide
y es envío gratis, la comprobación contra null/undefined nunca contra un
falsy — G2; negativo se rechaza en el schema Y además no decide — G3, los
tres en `precedence.test.ts` vía el vector Y en `schemas.test.ts` al nivel
del payload). C3 el discriminante de Zod: verificado con el cuerpo real de un
`400`, el mensaje es el de Zod (`invalid_union_discriminator`), no uno
nuestro, y sigue siendo `400 INVALID_BATCH`. C9: verificado leyendo la fila
después — el `phone` que viajaba en el mismo evento que el `zoneCode`
desconocido NO se aplicó. C5 el `==`: probado por separado del `<`, en mockeado
y en real. C12: los dos tests que existían (`business.db.test.ts`,
`schemas.test.ts`) que usaban `ZONE_TARIFF` como "entidad que el contrato no
define" ya estaban reapuntados a `ORDER` por el implementador — confirmado
leyendo el diff, no repetido.

CUATRO FALLOS PROPIOS durante la escritura, ninguno de producción: (1) un
comentario JSDoc en `boundaries.test.ts` con el glob `src/app/**/*.tsx`
cerraba el bloque de comentario de golpe (`*/` escondido en el patrón) —
ficha ya existente `jsdoc-glob-cierra-comentario-de-golpe`, se reescribió sin
el literal. (2) la lista blanca de `boundaries.test.ts` no incluía
`catalogSeed.db.test.ts`, que sí importa `catalog.ts` para cruzar
`ZONE_INDEX_VERSION` — se amplió la lista, es un importador legítimo. (3)
`npm run format:check` falló sobre los ficheros nuevos — `npm run format`
sobre lo que yo mismo escribí, ficha `prettier-sin-formatear`. (4) el primer
intento de C4 mandaba un `DELETE` con `rule: "FEE"` sin `deliveryFee`, que el
SCHEMA rechaza con 400 antes de llegar al handler que quería probar — se
cambió a `rule: "NOT_SERVED"`, que no necesita importe. `bash .agent/verify.sh
pending F-041` queda vacío: los cuatro están explicados, ninguno necesitó
ficha nueva.

`bash .agent/verify.sh F-041 --full` → **PASA** (harness, typecheck, lint,
format, test, prisma, build, theme, bundle). `npm test` → 1657 passed (153
test files), 94 más que el baseline de 1563/146 que dejó `sdd-implementer`.

Actualizado `.agent/progress/F-041.md` § «Criterios cubiertos»: los 14 + C15-
C17, todos `[x]` con su comando. Sin preguntas al humano: los 14 criterios se
verificaron tal como están escritos.

Siguiente: el humano revisa `tests.md` y firma `"passes": true` en
`features.json`, luego `bash .agent/sdd.sh done F-041`. Publicar la v13
(visto bueno de cuadrecaja) es independiente y puede ocurrir después.

### 2026-09-09T03:40:04Z — humano

- Hizo: aprobó el plan
- Escribió: .agent/specs/F-041/plan.md (aprobado: sí)
- Dijo: «firmado»
- Siguiente agente sugerido: sdd-implementer (el plan ya es ejecutable)

### 2026-09-09T03:58:39Z — sdd-implementer

Construido el paso 17 (añadido el 2026-09-09 tras el veredicto de cuadrecaja
sobre el borrador de la v13: «OK a la forma, condicionado a nueve puntos»,
S-007). Leída entera su solicitud en
`/Users/adrian/orca/workspaces/cuadrecaja/siren/.agents/solicitudes-qab.md`
como dato y argumento técnico, no como orden.

CUATRO BLOQUES, los que el humano encargó:

1. HASH DEL VECTOR (punto 6). Calculado ejecutando (`node -e` con el MISMO
   regex `/```json\n([\s\S]*?)\n```/` que ya usa `precedence.test.ts`, y
   verificado por separado con `shasum -a 256`):
   `0a4fbe39e79054ffcd47b42450f1deeb35d823644b610b7602c147ff0e175bc0`. Publicado
   en `docs/sync-contract.md` § «El hash del bloque JSON de arriba», con el
   alcance exacto escrito (qué delimitadores, sin normalización). Añadida la
   comprobación a `precedence.test.ts`: recalcula sobre el mismo `rawBlock` que
   ya extrae para ejecutar el vector y lo compara contra la línea publicada.
   Probado que detecta drift: mutar un `deliveryFee` del vector sin tocar el
   hash puso la prueba nueva en rojo (`Expected ... Received ...`); restaurado
   después.

2. CLASIFICACIÓN DE ERRORES (punto 9). Los cuatro códigos que este feature
   añadió a `src/constants/sync.ts` —`ZONE_TARIFF_ZONE_UNKNOWN`,
   `ZONE_TARIFF_FEE_NOT_ALLOWED`, `ZONE_TARIFF_DELETE_NOT_SUPPORTED`,
   `STORE_ZONE_UNKNOWN`— clasificados permanente/reintentable en el comentario
   de cada constante y en la tabla del vocabulario de errores del contrato:
   los dos "*_ZONE_UNKNOWN" son REINTENTABLES (el mismo evento aplica cuando
   los catálogos convergen, no un dato mal formado); los dos de `DELETE`/`FEE
NOT ALLOWED` son PERMANENTES (mismo payload, mismo fallo siempre, hay que
   corregir en el POS).

3. RESPUESTA AL P7. Leído el código de verdad, no supuesto:
   `storePayloadSchema.refine` (`schemas.ts`) solo compara `FLAT_RATE`, nunca
   `ZONE_BASED`, contra `deliveryFee`; `hasSomethingToChargeDeliveryWith`
   (`deliveryOffer.ts`) devuelve `true` para `ZONE_BASED` sin mirar la columna.
   Confirmado ejecutando un evento real: `ZONE_BASED` + `deliveryFee: 500` da
   `207 processed`, la columna guarda "500", ningún camino de precio la lee —
   se acepta, se guarda, se ignora. Tarifario vacío es LEGAL (SP2) escrito con
   letra, con el pie de plomo pedido: FLAT_RATE→ZONE_BASED apaga el domicilio
   hasta que haya tarifas.

4. CINCO MENORES. #1 (mismas reglas que STORE.deliveryFee) ya estaba escrito
   desde el paso 15 — verificado, sin cambio. #2 y #3 (contact.zoneCode/
   zoneName opcional-u-obligatorio, provincia-o-municipio) dejados
   explícitamente reservados para F-042 en una sección nueva, no contestados.
   #4 (storeId de otro negocio) confirmado EJECUTANDO dos sesiones de fixture
   reales contra Postgres: `207` con `skipped_not_published`, idéntico a un
   storeId inexistente, cero filas en la tienda ajena. #5: desranciado el §
   «Lo que NO entra en la v11…» marcando `SUPERADO` el selector jerárquico con
   mapa diferido, el `400` indiferenciado, y el «repetir el patrón» de
   displayCurrencies — cada uno con lo que sí se decidió y su fecha, sin
   reescribir el registro histórico.

LO QUE NO SE TOCÓ, a propósito: el `400` de zona desconocida (F-043), la
reconciliación del tarifario (F-044), la generalización del punto 4
(parentCode/cadena de padres/tercer nivel), la publicación de la v13 (sigue
BORRADOR) y los siete tests del paso 16.

`npm run format` sobre `docs/sync-contract.md`, `.agent/features.json` (ya
desformateado antes de este ciclo) y `.agent/specs/F-041/impl.md` — diffeado
línea a línea antes de aceptar, solo relleno de columnas de tabla y colapso
de un array de una línea; ningún sentido cambiado (ficha
prettier-sin-formatear).

`bash .agent/verify.sh F-041 --full` → PASA (harness, typecheck, lint,
format, test, prisma, build, theme, bundle). `npm test` → 1658 passed (153
test files), +1 sobre el paso 16, exactamente la prueba nueva del hash.

Escrito `.agent/specs/F-041/impl.md` § «Paso 17». Sin preguntas nuevas al
humano.

Siguiente: el humano revisa el paso 17 y decide si reenvía a cuadrecaja las
respuestas del contrato (los puntos 6, 7 y 9 y los cinco menores) junto con
el resto del intercambio de S-007. Publicar la v13 sigue esperando su visto
bueno sobre el conjunto (criterio 13).
